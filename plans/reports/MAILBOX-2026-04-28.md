# MAILBOX — Golden Triangle Review: PR #33
**Date**: 2026-04-28
**Scope**: fix(phase-3a): complete settings persistence, migration transaction, and play guard

---

## [P1-SUBMISSION] scouter → reviewer
**Phase**: 1 | **Task**: Scope & Context Mapping

### Changed Files (23 total, 6 primary code files)
- **DB**: `prisma/schema.prisma` (+10 cols), `prisma/migrations/20260427191924_...sql` (new)
- **API**: `app/api/children/[id]/settings/route.ts` (allowed ×19), `app/api/children/migrate/route.ts` ($transaction + copy)
- **Hook**: `lib/hooks/use-settings.ts` (DB fetch on mount, debounced PATCH)
- **UI**: `app/(child)/play/[gameType]/[lessonId]/page.tsx` (guard inside useEffect, `''` sentinel)
- **Tests**: `__tests__/api/children-migrate.test.ts` (mockTx + $transaction)
- **Docs/Plans**: 13 files (lower priority)

### Critical Blast Radius Findings
- `lib/types/common.ts::ChildSettings` — NOT updated, missing 10 new fields (HIGH GAP)
- `lib/types/api.ts::UpdateChildSettingsRequest` — NOT updated (MEDIUM GAP)
- `app/(child)/layout.tsx` — Calls migrate POST but may NOT pass `guestId`; if missing, $transaction copy is unreachable (HIGH GAP)
- `lib/hooks/use-game-session.ts` — Empty string `''` sentinel not guarded (only `guest_` prefix checked at line 38) (HIGH GAP)
- `parent-settings-content.tsx` — Fires its own PATCH + useSettings now also fires PATCH = potential race condition (MEDIUM)

### Architecture Layers Touched
DB → API → Hook → UI (Child) | Types layer NOT updated

---

## [P1-REVIEW] tech-lead → scouter | Status: FAIL | Round: 1/3

All 4 scouter HIGH gaps confirmed + 2 additional gaps found:

| Gap | Sev | Evidence |
|-----|-----|----------|
| `lib/types/common.ts::ChildSettings` missing 10 fields | HIGH | lines 48–58 — only 9 fields |
| `lib/types/api.ts::UpdateChildSettingsRequest` missing fields | HIGH (upgraded) | lines 32–42 — stale, never imported |
| `app/(child)/layout.tsx` no `guestId` in migrate POST | HIGH | lines 134–139 — body omits guestId → silent data loss |
| `use-game-session.ts` empty `''` not guarded | HIGH | line 38 — only `guest_` prefix checked |
| `parent-settings-content.tsx` DEFAULTS object stale | HIGH (NEW) | lines 28–31 — typed as ChildSettings, missing 10 fields |
| `use-settings.ts` bedtime hour `20` vs DB default `21` | MEDIUM (NEW) | line 26 vs schema line 74 |

Required: scouter accept gaps 5–6 and confirm scope complete.

---

## [P1-DEFENSE] scouter → tech-lead | Round: 1
Accepting all 6 gaps — evidence is concrete with file:line citations. No disputes.
Gap 5 (DEFAULTS stale): Valid — cascades compile errors once ChildSettings type is fixed.
Gap 6 (bedtime hour mismatch): Valid — `20` vs `21` causes first-load flicker for new users.

---

## [P1-APPROVAL] tech-lead → reviewer
All gaps accepted. Scope complete. Phase 1 DECISION: produce SCOUT-pr33 deliverable.

✅ CONSENSUS: reviewer ✓ | scouter ✓ | tech-lead ✓

---

## [P2-SUBMISSION] reviewer(exec) → security+perf
Phase 2 deep review complete. 30 findings across 6 files.
**Block-merge issues**: G3 (guestId data loss), G4 (empty string guard), R18 (no range validation)
**HIGH**: R1, R2, R7, R8, R12, R13, R17, R19, R26, R27, G1–G5

---

## [P2-REVIEW] security+perf → reviewer(exec) | Status: FAIL | Round: 1/3

**Disputes**:
- R7/G4: severity LOW (API rejects empty string with 400; play page guard fires before startSession in practice)
- R8: severity MEDIUM not HIGH (correctness issue, not security)
- R19: severity LOW (documented graceful degradation)

**CRITICAL finding missed by executor**:
- NEW-S1 CRITICAL: `POST/GET/PATCH /api/sessions` + `POST /api/sessions/[id]/attempts` — ZERO auth. Any unauthenticated caller can create sessions for any childId, submit fake attempts, and trigger star/sticker awards. Complete OWASP A01 violation.

**Additional findings**: NEW-S2 (attempts no auth), NEW-S3 (children POST no name length/color allowlist), NEW-S4 (login no rate limit), NEW-S5 (answer no length bound), NEW-S6 (cookie stores raw DB PK), NEW-S7 (in-memory rate limiter), NEW-S8 (no CSP header)

---

## [P2-DEFENSE] reviewer(exec) → security+perf | Round: 1

**Accepted**:
- R7/G4: Downgrade to LOW — API rejection at session layer is valid defense
- R8: Downgrade to MEDIUM — correctness issue confirmed
- R19: Downgrade to LOW — graceful degradation pattern
- NEW-S1: ACCEPT as CRITICAL — sessions API auth gap is confirmed; this was outside the scope of changed files in PR #33 (pre-existing) but must be surfaced
- NEW-S2–S8: All accepted

**Disputed**: None. All findings are valid.

---

## [P2-APPROVAL] security+perf → reviewer(exec)
All disputes resolved. NEW-S1 confirmed as pre-existing CRITICAL. Phase 2 findings consolidated.

---

## [P3-SUBMISSION] planner → reviewer
REVIEW-REPORT-pr33.md created: 3 BLOCK items, 11 HIGH, 15 WARN, dependency order specified.

---

## [P3-REVIEW] reviewer → planner | Status: FAIL | Round: 1/3

5 challenges, 3 require plan corrections:
1. **NEW-S1 fix incomplete** — `guest_` prefix bypass not closed: attacker can send `guest_<real-uuid>` to bypass auth. Fix must include DB lookup verifying guest_ prefixed IDs don't match real Child records.
2. **R2 fix misleading** — moving findFirst inside tx doesn't prevent race under PostgreSQL READ COMMITTED. Plan must state R1+R2 required together (P2002 catch is the true safety net).
3. **R15 incorrectly downgraded** — multi-profile unmount race IS data corruption in Phase 3C context. Promote back to HIGH.
4. **R12 migration code missing** — existing users lose settings on deploy. Spec must include localStorage migration code.
5. **G3 fix mostly correct** — add null-safety note: `guestId: state.childId ?? undefined`.

---

## [P3-DEFENSE] planner → reviewer | Round: 1
Accepting all 5 corrections. Updating REVIEW-REPORT-pr33.md with: (1) NEW-S1 guest-bypass fix, (2) R1+R2 dependency note, (3) R15 promoted to HIGH, (4) R12 migration code, (5) G3 null-safety.

---

## [P3-APPROVAL] reviewer → tech-lead
Plan corrections accepted. Phase 3 DECISION: REVIEW-REPORT-pr33 is complete and accurate.

✅ CONSENSUS: tech-lead ✓ | planner ✓ | reviewer ✓

---

## [P4-SUBMISSION] reporter → business-analyst
REVIEW-pr33.md written with verdict, risk table, 3 block-merge, 11 HIGH, 15 WARN, security summary, plan compliance, recommended actions.

---

## [P4-REVIEW] business-analyst → reporter | Status: FAIL | Round: 1/3
4 issues:
1. Executive summary doesn't lead with plain-language data loss impact
2. Non-technical readers can't parse IDOR/TOCTOU/P2002 — need plain-language addition
3. Recommended actions say "Owner: dev" — not assignable to roles
4. localStorage risk likelihood calibrated MEDIUM but should be HIGH for shared family devices

---

## [P4-DEFENSE] reporter → business-analyst | Round: 1
Accepting all 4 corrections:
1. Executive summary rewritten to lead with plain-language data loss statement
2. Plain-language sentence added before technical summary
3. Recommended actions updated to frontend-engineer / backend-engineer / tech-lead
4. localStorage collision risk updated to HIGH likelihood

---

## [P4-APPROVAL] business-analyst → tech-lead
All corrections applied. Report is stakeholder-ready.

✅ CONSENSUS: tech-lead ✓ | reporter ✓ | business-analyst ✓

---
