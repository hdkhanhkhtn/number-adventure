## SUBMISSION P2-CORRECTNESS-ARCH-QUALITY

**Executor:** code-reviewer subagent
**Date:** 2026-04-24
**Scope:** Deep review — prisma schema, context providers, UI components, types, static config, API stubs, CSS, layout (17 files)

---

# Phase 2 Review — Correctness, Architecture, Quality

---

## Dimension: CORRECTNESS

| # | File:Line | Issue | Severity | Fix |
|---|-----------|-------|----------|-----|
| C1 | `context/audio-context.tsx:9-11` | `voiceStyle` and `kidLang` exposed in interface as readable state but hardcoded constants — no `setVoiceStyle`/`setKidLang` mutators. Components calling `updateSettings({voiceStyle:'Calm'})` via GameProgressContext diverge silently from AudioContext's hardcoded values. Two sources of truth for same setting. | HIGH | Add `setVoiceStyle`/`setKidLang` to interface and provider, or remove fields and derive from `GameProgressContext.settings` |
| C2 | `context/audio-context.tsx:40` | `utterance.lang = 'en-US'` hardcoded — ignores the `kidLang` field exposed in the same interface. If `kidLang` is ever `'vi'`, speech still reads in English. | HIGH | Use `kidLang` state: `utterance.lang = kidLang === 'vi' ? 'vi-VN' : 'en-US'` |
| C3 | `components/ui/bap-mascot.tsx:91-92` | Both eye slots render `{EYES[mood]}` identically. For `wink` mood, both eyes become the closed arc — Bắp looks asleep, not winking. The intent is clearly one eye open, one closed. | HIGH | Use separate maps: left eye uses `EYES[mood]`, right eye uses a `EYES_RIGHT` map where `wink` stays as the happy right-eye ellipse fragment |
| C4 | `components/ui/num-tile.tsx:41,54` | `state === 'disabled'` sets `pointerEvents: 'none'` via CSS but does not pass HTML `disabled` attribute to `<button>`. Screen readers do not know the button is disabled; keyboard users can still Tab to it and trigger Enter/Space (click fires despite `pointerEvents:none` for keyboard nav). | HIGH | Add `disabled={state === 'disabled'}` to the `<button>` element |
| C5 | `components/ui/big-button.tsx:42` | `onClick={disabled ? undefined : onClick}` does not set HTML `disabled` attribute. The `<button>` is keyboard-focusable and pressing Enter fires silently, while screen readers see an enabled interactive button. | HIGH | Add `disabled={disabled}` to the `<button>` element |
| C6 | `components/ui/num-tile.tsx:84-93` | Touch events (`onTouchStart`, `onTouchEnd`) have no `e.preventDefault()`. Mobile browsers synthesize a `click` ~300ms after touch, causing `onClick` to fire twice: once via style-change path and once via synthetic click. | WARN | Add `e.preventDefault()` to `onTouchStart` in both `num-tile.tsx` and `big-button.tsx` |
| C7 | `context/game-progress-context.tsx:44-45` | `CLEAR` returns `INITIAL_STATE`, which triggers the write-through `useEffect`, writing `{childId:null,...}` back to localStorage instead of removing the entry. Not a crash bug, but clean logout should remove the key. | WARN | Add `localStorage.removeItem(LS_KEY)` in the CLEAR path (separate `useEffect` watching `state.childId === null` or within the dispatch helper) |
| C8 | `context/game-progress-context.tsx:108-116` | The `ctx` object is reconstructed on every render with new function references. All consumers of `useGameProgress()` re-render whenever any state field changes, even if they only use `setChild`. | WARN | Wrap `ctx` in `useMemo` and each dispatch function in `useCallback` |
| C9 | `context/theme-context.tsx:29` | `useState<ThemeName>(readStoredTheme)` passes `readStoredTheme` as lazy initializer — runs at first render. Server renders `'garden'` (SSR guard), client may read stored theme, producing hydration mismatch for users with non-default theme. | WARN | Initialize state with `'garden'` always; apply stored theme via `useEffect` after mount |
| C10 | `lib/types/api.ts:57` | `SubmitAttemptRequest.correct: boolean` is client-supplied with no warning comment that server must re-validate. If Phase B implements the endpoint and stores this field directly, it is a score-tampering vector. | CRITICAL | Add JSDoc: `/** MUST NOT trust — server must re-evaluate answer against stored question */`; Phase B plan must require server-side re-computation |
| C11 | `lib/types/api.ts:51` | `CompleteSessionRequest.stars: number` — no range constraint (0–3 per schema). Client could send `stars: 999`. | WARN | Add comment `// must be 0–3`; enforce range validation in Phase B handler |
| C12 | `prisma/schema.prisma` (all relations) | No `onDelete` cascade rules on any relation. Deleting a `Child` is blocked by Prisma's default `Restrict` on `ChildSettings`, `GameSession`, `GameAttempt`, `ChildSticker`, `Streak`. No documented delete strategy. | HIGH | Add `onDelete: Cascade` to all child-scoped relations, or document intended delete behavior explicitly |
| C13 | `prisma/schema.prisma:26` / `lib/types/api.ts:29` / `lib/types/common.ts:46` | `Child.color` DB default is `"sun"` — plan spec says `@default("sage")`. `ChildProfile.color` is typed `MascotColor` (union). `CreateChildRequest.color` is `string?` (untyped). Three layers disagree. | WARN | Align: schema default to `"sage"` per plan; `CreateChildRequest.color` to `MascotColor?` |

---

## Dimension: ARCHITECTURE COMPLIANCE

| # | File:Line | Issue | Severity | Fix |
|---|-----------|-------|----------|-----|
| A1 | All 14 `app/api/**/route.ts` | All route handlers have empty signatures `POST()`, `GET()` — ignoring the `request` parameter entirely. The plan Step 11 example explicitly shows `POST(req: NextRequest)`. Phase B must modify every file to add the parameter. | HIGH | Add `(_req: NextRequest)` or `(req: NextRequest)` to all handler signatures now |
| A2 | `app/api/` — missing routes | Plan specifies `/api/worlds/route.ts`, `/api/lessons/[lessonId]/route.ts`, `/api/streaks/[childId]/route.ts`. None exist. Three of the 11 required route stubs absent. | HIGH | Create the three missing stub routes; Phase B depends on their structure |
| A3 | `app/api/stickers/route.ts` vs plan | Plan specifies `/api/stickers/[childId]/route.ts`. Implemented as `/api/stickers/route.ts` (no child scoping) with a separate non-plan path `/api/children/[id]/stickers/route.ts`. The plan path is absent. | WARN | Add `/api/stickers/[childId]/route.ts` per plan, or document the path change as an intentional deviation with rationale |
| A4 | `app/page.tsx:4` | `redirect('/home')` targets non-existent `app/home/` directory (404). Plan acceptance criteria 4 explicitly allows this ("404 OK since page not created yet") — tracked for Phase B. | INFO | Phase B dependency; not a Phase A blocker per plan acceptance criteria |
| A5 | `app/globals.css:131-133` | `@theme inline` block maps `--shadow-card: var(--shadow-card)` — a self-referencing Tailwind v4 variable. In `@theme inline`, the property name defines the design token; `var(--shadow-card)` resolves to itself, yielding `undefined` in computed styles. All box shadows are silently broken. | CRITICAL | In `@theme inline`, rename shadow tokens with a distinct prefix: e.g., `--drop-shadow-card: var(--shadow-card)` to avoid the name collision |
| A6 | `app/layout.tsx:9,16,23` vs `app/globals.css:58-60` | `next/font` registers variables `--font-fredoka`, `--font-baloo2`, `--font-be-vietnam-pro`. But `globals.css` sets `--font-kid` as a literal fallback stack string, not consuming the next/font CSS variables. The optimized subset fonts load but are never referenced. | WARN | Change globals.css font vars to: `--font-kid: var(--font-fredoka), var(--font-baloo2), ui-rounded, system-ui, sans-serif` |
| A7 | `prisma/schema.prisma:55` | `Lesson.worldId` is a plain `String` with no FK constraint (no `World` table — by design per plan). No DB-level enforcement that `worldId` is a valid world. A seeding error could create orphaned lessons. | WARN | Add a DB check constraint via Prisma `@db.VarChar` + enum, or document as accepted trade-off (static data = no FK needed) |
| A8 | `src/data/game-config/game-types.ts:47-49` | Both `counting-castle` and `number-sequence` worlds use `gameType: 'number-order'`. No `variant` or `subType` field distinguishes sequence-pattern from counting-order lessons. Game engine will receive the same `gameType` string for two conceptually distinct activities. | WARN | Add optional `variant?: string` to `LessonTemplate` to allow differentiation, or document that the AI question `payload` carries the behavioral distinction |
| A9 | `context/audio-context.tsx` | Plan specifies "React Context + useReducer pattern" for all providers. `AudioContext` uses `useState` only. As a stub this is acceptable, but the pattern divergence should be noted for Phase D. | INFO | Note for Phase D: migrate to `useReducer` when full audio state (queue, preloading) is added |
| A10 | `prisma/` directory | No `prisma/migrations/` directory exists — migration has not been run and committed. Plan acceptance criterion 2 requires `npx prisma migrate dev` to succeed. Cannot confirm AC2. | HIGH | Run `npx prisma migrate dev --name init` and commit the generated `prisma/migrations/` directory |
| A11 | `lib/cn.ts` AND `lib/utils/cn.ts` | Two copies of the `cn` utility at different paths. `lib/cn.ts` and `lib/utils/cn.ts` both exist. Plan specifies `lib/utils/cn.ts`. | WARN | Remove `lib/cn.ts`; verify all imports point to `lib/utils/cn.ts` |

---

## Dimension: CODE QUALITY

| # | File:Line | Issue | Severity | Fix |
|---|-----------|-------|----------|-----|
| Q1 | `num-tile.tsx:69-93`, `big-button.tsx:62-83` | Mouse and touch press handlers duplicated across both components — same 5-event pattern (onMouseDown/Up/Leave, onTouchStart/End) repeated with identical logic. ~25 lines each, ~50 lines total duplicate. | WARN | Extract `usePressEffect(disabled, baseShadow, pressedShadow)` hook returning the 5 event handler props |
| Q2 | `num-tile.tsx:20-27`, `big-button.tsx:15-28` | `COLOR_MAP` and `SIZE_MAP` defined inline per component. Hardcoded hex colors duplicated from design tokens. As more components are added, colors diverge from the design system. | WARN | Extract to `lib/constants/tile-styles.ts` and `lib/constants/button-styles.ts` |
| Q3 | `components/ui/streak-card.tsx:9` | `DAYS = ['M', 'T', 'W', 'T', 'F', 'S', 'S']` — Tuesday and Thursday both render as 'T'. Users cannot distinguish them visually. | WARN | Use `['M', 'Tu', 'W', 'Th', 'F', 'Sa', 'Su']` |
| Q4 | `context/game-progress-context.tsx:82` | `JSON.parse(cached) as Partial<GameProgressState>` casts without runtime validation. Corrupted or injected localStorage values could crash on property access or inject wrong types for `currentWorldId` (union), `profile.age` (number), etc. | HIGH | Add a shape validation guard function before dispatching hydration actions (minimum: check `typeof` on critical fields) |
| Q5 | `context/audio-context.tsx:42` | `window.speechSynthesis.cancel()` before every `speak()`. Rapid taps cause each call to cancel the previous, resulting in silence if taps come faster than the utterance starts. No debounce or queue. | WARN | Add ~200ms debounce on `speak()` using `useRef` for the timer ID |
| Q6 | `lib/types/common.ts:69` | `WeekProgress.days: boolean[]` is unbounded — 5 or 10 booleans typecheck. Comment says "7 booleans" but is unenforced. | INFO | Use `[boolean, boolean, boolean, boolean, boolean, boolean, boolean]` tuple type |
| Q7 | `lib/types/api.ts:69-72` | `AIQuestionPayload.payload: Record<string, unknown>` is too loose for Phase B. The game engine will need to discriminate by `gameType`. Absence of `gameType` in the payload type will make the engine harder to typecheck. | WARN | Add `gameType: GameType` to `AIQuestionPayload` now |
| Q8 | `app/globals.css:154` | `body { overflow: hidden }` globally disables scrolling. Parent dashboard or long-content screens will silently break without the `.scroll` utility class. No comment documents this. | WARN | Add comment: `/* Mobile viewport containment — use .scroll class on any scrollable child container */` |
| Q9 | `src/data/game-config/worlds.ts:8` | `WorldConfig.bg` typed as `string` — no enforcement that it's a valid hex color. `'B9E2B2'` (missing `#`) would silently produce a broken background. | INFO | Add comment `// must be a valid hex color e.g. '#RRGGBB'` or a template literal type |

---

## Plan Compliance Check (Phase A — phase-A-foundation.md)

| Phase-A Requirement | Status | Evidence |
|---|---|---|
| Next.js 14, TypeScript strict | PASS | `tsconfig.json` `"strict": true` confirmed |
| Docker Compose + .env.example | PARTIAL | Compose has healthcheck improvement; missing `Dockerfile`; `AI_ENDPOINT` hardcoded production URL |
| Prisma schema 10 tables | PASS | All 10 models present |
| Migration run + committed | FAIL | No `prisma/migrations/` directory |
| Prisma client singleton | PASS | `lib/prisma.ts` correct + dev logging improvement |
| globals.css tokens + keyframes | PASS with defect | All tokens present; shadow `@theme` name collision (A5) |
| Shared types (`common.ts`, `api.ts`) | PASS | No `any` types; complete coverage |
| cn utility | PASS with defect | Exists at two paths; plan path `lib/utils/cn.ts` is correct |
| Static game config (5 files) | PASS | All 5 files present and typed |
| UI components (14 files) | PASS | All 14 present; all under 200 lines |
| Context providers (3 + composite) | PASS with defects | All created; AudioContext has hardcoded values (C1, C2) |
| API route scaffold (11 routes) | PARTIAL | 14 files created; 3 plan-specified paths missing; all missing `request` param |
| Root layout + entry redirect | PASS | `layout.tsx` and `page.tsx` correct |
| Build verification | UNKNOWN | No build log evidence |

**Acceptance Criteria:**
- AC1 (build zero errors): UNKNOWN
- AC2 (migration): FAIL
- AC3 (prisma studio): UNKNOWN
- AC4 (redirect): PASS
- AC5 (typed props): PASS
- AC6 (25+ CSS vars): PASS
- AC7 (3 themes): PASS
- AC8 (localStorage cache): PASS
- AC9 (data-theme): PASS
- AC10 (API routes respond): PASS
- AC11 (typed config): PASS
- AC12 (files under 200 lines): PASS

---

## Positive Findings

- `lib/prisma.ts` — adds conditional dev logging beyond plan spec; clean singleton pattern
- `docker-compose.yml` — adds `healthcheck` and `depends_on: condition: service_healthy`, preventing race conditions
- `context/game-progress-context.tsx` — correctly excludes `sessionActive` from localStorage cache (only persistent fields serialized)
- `prisma/schema.prisma` — `ChildSticker.@@unique([childId, stickerId])` prevents duplicate sticker awards
- `components/ui/bap-mascot.tsx` — pure SVG, zero external image dependencies, `aria-label` present, all 5 moods and 5 colors implemented
- All component files under 200 lines — file size rule fully respected
- `lib/types/common.ts` + `api.ts` — no `any` types; strict TypeScript throughout
- `src/data/game-config/sticker-defs.ts` — 40 stickers across 5 worlds with well-distributed rarity tiers

---

## Summary of Blockers for Phase B

| # | Issue | File | Why Blocking |
|---|-------|------|--------------|
| B1 | `SubmitAttemptRequest.correct` client-supplied (C10) | `lib/types/api.ts:57` | Phase B will implement the attempts endpoint — if this field is accepted, score tampering is embedded |
| B2 | `@theme inline` shadow self-reference (A5) | `app/globals.css:131-133` | All shadow styling silently broken in current build |
| B3 | No `prisma/migrations/` (A10) | `prisma/` | DB cannot be provisioned; Phase B has no runnable database |
| B4 | All route handlers missing `request` param (A1) | 14 route files | Phase B must touch every route file to add the param; cleaner to fix now |
| B5 | 3 plan-specified routes absent (A2) | `app/api/` | `worlds`, `lessons`, `streaks` routes needed for Phase B world/lesson/streak flows |
| B6 | No cascade deletes on schema (C12) | `prisma/schema.prisma` | Blocking parent/child deletion in Phase C |
| B7 | `localStorage` parse without validation (Q4) | `game-progress-context.tsx:82` | Corrupted cache can crash the app silently on any screen load |

---

## REVIEW P2-SECURITY-PERFORMANCE

**Reviewer**: Security Engineer (security + performance lens)
**PR**: #3 — Phase A Foundation
**Date**: 2026-04-24
**Files reviewed**: 18 files (all 14 API route stubs, lib/types/api.ts, lib/types/common.ts, prisma/schema.prisma, docker-compose.yml, .env.example, context/game-progress-context.tsx, next.config.ts, package.json, .gitignore)

---

# Phase 2 Review — Security + Performance

## SECURITY FINDINGS

| # | OWASP Category | File:Line | Issue | Severity | Fix |
|---|----------------|-----------|-------|----------|-----|
| S1 | A01 Broken Access Control | All 14 `app/api/**/route.ts`:4 | Zero auth middleware — every route handler has empty signature `POST()` / `GET()` with no `request` param. When implemented, there is no centralized auth guard. No `middleware.ts` exists to protect any route. | CRITICAL | Create `middleware.ts` at project root that validates session token on all `/api/*` routes except `/api/auth/login` and `/api/auth/register`. Add `(req: NextRequest)` parameter to every handler before Phase B implementation begins. |
| S2 | A01 Broken Access Control | `app/api/progress/[childId]/route.ts`:4, `app/api/report/[childId]/route.ts`:4, `app/api/children/[id]/settings/route.ts`:4, `app/api/children/[id]/stickers/route.ts`:4 | URL parameter `childId` / `id` is never validated against the authenticated parent's children. Any authenticated parent can query `GET /api/progress/ANY_CHILD_ID`. Classic IDOR. | CRITICAL | When implementing: always JOIN child against parent via `WHERE child.parentId = authenticatedParentId`. Never trust URL params as ownership proof. |
| S3 | A04 Insecure Design | `lib/types/api.ts`:57 | `SubmitAttemptRequest.correct: boolean` is a client-supplied field. The client declares whether its own answer is correct. Server must never accept this value — correctness must be computed server-side by comparing `answer` against the stored `AIQuestion.payload`. | CRITICAL | Remove `correct` from `SubmitAttemptRequest`. Compute server-side: load question by `questionId`, evaluate `answer`, derive boolean. |
| S4 | A07 Auth Failures | `lib/types/api.ts`:21-23 | `PinRequest.pin` is typed as `string` with comment "4-digit" but there is no length/format constraint in the type definition. Without explicit server-side validation the field can receive arbitrary-length strings — bcrypt comparison on a very long string is a known DoS vector (bcrypt truncates at 72 bytes, but CPU spike before truncation is real). | HIGH | Enforce `pin.length === 4 && /^\d{4}$/.test(pin)` server-side before calling `bcrypt.compare`. Also apply rate limiting (≤5 attempts per 15 min per parentId) on `POST /api/auth/pin`. |
| S5 | A07 Auth Failures | `lib/types/api.ts`:10-13 | `LoginRequest` has no field for device fingerprint or IP throttling signal. No rate-limiting dependency is present in `package.json`. Brute-force login is unmitigated at the type/dependency level. | HIGH | Add `express-rate-limit` equivalent for Next.js (e.g., `@upstash/ratelimit` + Redis, or Vercel's edge middleware approach). 5 attempts / 15 min per IP minimum for `/api/auth/login`. |
| S6 | A02 Cryptographic Failures | `lib/types/api.ts`:4-8 | `RegisterRequest.password: string` — no minimum length or complexity constraint encoded. Server implementation must enforce ≥8 chars. More critically, `AuthResponse` (line 15-19) returns `parentId` and `email` — ensure no JWT token is ever embedded directly in `AuthResponse`; use HttpOnly cookies instead. | HIGH | Enforce server-side: password min 8 chars. Issue session as HttpOnly, SameSite=Strict cookie, not in JSON body. `AuthResponse` must never contain the raw session token. |
| S7 | A05 Security Misconfiguration | `next.config.ts`:1-7 | `nextConfig` is completely empty — no security headers configured. Missing: `X-Frame-Options`, `X-Content-Type-Options`, `Content-Security-Policy`, `Referrer-Policy`, `Permissions-Policy`. A children's app without CSP is exposed to XSS content injection risks. | HIGH | Add `headers()` in `next.config.ts` with at minimum: `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Content-Security-Policy: default-src 'self'`, `Referrer-Policy: no-referrer`. |
| S8 | A05 Security Misconfiguration | `docker-compose.yml`:9 | `POSTGRES_PASSWORD: bap_dev_pass` hardcoded in compose file as plaintext. While this is labeled dev, the same file has `build: .` suggesting it may be used for production deploys. If committed and reused, this credential is exposed. | HIGH | Use `${POSTGRES_PASSWORD}` env variable reference in compose. Ensure production compose uses Docker secrets or external secret injection, never plaintext in YAML. |
| S9 | A05 Security Misconfiguration | `docker-compose.yml`:25 | `AI_ENDPOINT: https://9router.remotestaff.vn/v1` hardcoded production endpoint URL. Endpoint URL encodes deployment topology knowledge and should not be baked into source control. | HIGH | Replace with `AI_ENDPOINT: ${AI_ENDPOINT}` and require the value to be injected at deploy time via `.env` or CI secret. |
| S10 | A05 Security Misconfiguration | `docker-compose.yml`:5-6 | Port `5432` is exposed on `0.0.0.0` (default), making the PostgreSQL instance reachable from the host network interface — dangerous on a VPS without a firewall. | WARN | Restrict to `127.0.0.1:5432:5432` for local dev, or remove the `ports` mapping entirely for production (app container reaches db via internal Docker network only). |
| S11 | A09 Logging Failures | All `app/api/**/route.ts`:8 | Every catch block swallows the error silently — `catch { return ... }`. No structured logging. When real logic is added, failures will be invisible. | WARN | Add structured logger (e.g., `pino`). Log at minimum: timestamp, route path, error message (no stack trace to client). Never log passwords, tokens, or child PII. |
| S12 | A10 SSRF | `app/api/ai/generate-questions/route.ts`:4 | When implemented, this route will forward to `AI_ENDPOINT` from env. If the endpoint value is not validated to an allowlist (e.g., only `remotestaff.vn` domain), a misconfigured env var could point to an internal service — SSRF via config injection. | WARN | Validate `AI_ENDPOINT` at startup: parse URL, check hostname against allowlist, reject if not matching. |
| S13 | A08 Data Integrity Failures | `docker-compose.yml`:21 | `build: .` with no `Dockerfile` present in the repository. Docker will fail to build, or an attacker-supplied `Dockerfile` in the repo root could be used in CI. | WARN | Either add a Dockerfile with pinned Node base image (`node:20-alpine`) and non-root user, or remove `build: .` and use a pre-built image reference. |
| S14 | A05 Security Misconfiguration | `.env.example`:2 | `AI_ENDPOINT=https://9router.remotestaff.vn/v1` — real production endpoint URL is committed to the example env file. This leaks deployment infrastructure details publicly. | WARN | Replace with a placeholder: `AI_ENDPOINT=https://your-ai-endpoint.example.com/v1`. |
| S15 | A01 Broken Access Control | `app/api/sessions/[id]/attempts/route.ts`:4 | `POST /api/sessions/:id/attempts` — when implemented, the session `id` in URL must be validated to belong to the authenticated child's active session. Without this, any session ID can receive attempt submissions, corrupting other children's records. | WARN | When implementing: verify `session.childId` matches authenticated child before inserting `GameAttempt`. |

---

## PERFORMANCE FINDINGS

| # | Category | File:Line | Issue | Severity | Fix |
|---|----------|-----------|-------|----------|-----|
| P1 | Missing DB Index | `prisma/schema.prisma`:22-35 | `Child` model has `parentId String` with a relation but no `@@index([parentId])`. `GET /api/children` will do a sequential scan on the `Child` table filtered by `parentId`. With many families this is a table scan. | HIGH | Add `@@index([parentId])` to `Child` model. |
| P2 | Missing DB Index | `prisma/schema.prisma`:66-78 | `GameSession` has `childId String` and `lessonId String` with no indexes. `GET /api/progress/:childId` and session queries will both trigger seq scans. | HIGH | Add `@@index([childId])` and `@@index([childId, status])` to `GameSession`. |
| P3 | Missing DB Index | `prisma/schema.prisma`:80-91 | `GameAttempt` has `sessionId String` with no index. Fetching all attempts for a session requires a full scan of the attempts table. | HIGH | Add `@@index([sessionId])` to `GameAttempt`. |
| P4 | Missing DB Index | `prisma/schema.prisma`:93-103 | `AIQuestion` has `lessonId String`, `gameType String`, `difficulty String` — all likely query dimensions for caching lookups. No indexes. | WARN | Add `@@index([lessonId, gameType, difficulty])` to `AIQuestion`. |
| P5 | Missing DB Index | `prisma/schema.prisma`:115-125 | `ChildSticker.childId` has no standalone index (only the `@@unique([childId, stickerId])` composite). Listing all stickers for a child uses the composite unique index which is adequate. This is acceptable. | INFO | No action needed — the composite unique index covers child sticker listing. |
| P6 | N+1 Query Risk | `prisma/schema.prisma`:22-35, `app/api/children/route.ts` | `GET /api/children` — when implemented, fetching children with their settings requires eager loading. Without `include: { settings: true }`, the implementation will produce 1 query for children + N queries for settings. | HIGH | When implementing: always use `prisma.child.findMany({ where: { parentId }, include: { settings: true } })` in a single query. |
| P7 | N+1 Query Risk | `prisma/schema.prisma`:66-91, `app/api/progress/[childId]/route.ts` | `GET /api/progress/:childId` — progress calculation needs sessions + attempts. Without a single aggregation query, naive implementation would fetch sessions then loop fetching attempts per session. | HIGH | When implementing: use Prisma's `_count` aggregation or a single raw GROUP BY query. Do not loop. |
| P8 | Context Re-render | `context/game-progress-context.tsx`:94-106 | The write-through `useEffect` at line 94 writes to `localStorage` on every state change. The `ctx` object at line 108 is re-created on every render (new object reference each time). Any component using `useGameProgress()` will re-render on every single state change, even if the consumed slice hasn't changed. | WARN | Memoize `ctx` with `useMemo`, or split context into separate contexts per concern (profile, settings, session). `useMemo` fix: `const ctx = useMemo(() => ({ state, setChild, ... }), [state])`. |
| P9 | Bundle Size | `package.json`:13 | `framer-motion: ^12.38.0` — Framer Motion v12 is ~45KB gzipped. It appears in `dependencies` (not lazy). If imported at the root layout or any non-code-split boundary, it bloats the initial JS bundle for a mobile-first children's app. | WARN | Import Framer Motion components only in leaf game components, never in layout or shared providers. Use `next/dynamic` with `ssr: false` for animation-heavy game screens. |
| P10 | AI Cost / Rate Control | `prisma/schema.prisma`:93-103, `app/api/ai/generate-questions/route.ts` | The `AIQuestion` table exists (good — caching design is present in schema). However there is no `@@unique([lessonId, gameType, difficulty])` constraint, meaning duplicate questions for the same lesson+type+difficulty can accumulate, and the cache lookup must rely on application-level dedup logic. | WARN | Add `@@unique([lessonId, gameType, difficulty])` or a `expiresAt` column to `AIQuestion` to enable: (1) upsert-based caching, (2) TTL-based regeneration. Also enforce server-side `count` max (e.g., ≤20) in `GenerateQuestionsRequest`. |
| P11 | Missing `count` Bound | `lib/types/api.ts`:62-67 | `GenerateQuestionsRequest.count?: number` is optional with no upper bound. When implemented, a request with `count: 10000` would invoke the AI endpoint 10000 times or attempt to generate a 10000-item payload. | HIGH | Enforce server-side: `const safeCount = Math.min(req.count ?? 10, 20)`. Never trust client-supplied count. |

---

## Infrastructure Security

| # | File:Line | Issue | Severity | Fix |
|---|-----------|-------|----------|-----|
| I1 | `docker-compose.yml`:1 | No `Dockerfile` exists in repo but `build: .` references one. Docker build will fail. No base image is pinned, no non-root user is defined. | CRITICAL | Create `Dockerfile` with: `FROM node:20-alpine`, non-root user (`addgroup/adduser`), multi-stage build (builder + runner stages), `COPY --chown=node:node`. |
| I2 | `docker-compose.yml`:23-26 | `app` service has no `restart: unless-stopped` policy. On VPS, a crash leaves the app down permanently. No health check for the app container. | WARN | Add `restart: unless-stopped` and a healthcheck: `test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]`. Add a `GET /api/health` route. |
| I3 | `docker-compose.yml`:24 | `DATABASE_URL` contains `bap_dev_pass` inline. This is the same value as the hardcoded `POSTGRES_PASSWORD`. Both must be extracted to env vars. | HIGH | Use `DATABASE_URL: postgresql://bap:${POSTGRES_PASSWORD}@db:5432/bap_number_adventure` and inject `POSTGRES_PASSWORD` via `.env` (gitignored) or Docker secrets. |
| I4 | `next.config.ts`:1-7 | No `output: 'standalone'` configured. For Docker deployment this means `node_modules` must be copied entirely into the image, bloating image size (typically 500MB+). | WARN | Add `output: 'standalone'` to `nextConfig`. This produces a minimal self-contained output that dramatically reduces Docker image size. |
| I5 | `.env.example`:1 | `DATABASE_URL` uses `localhost` but in Docker the host should be `db` (service name). The example will mislead developers who copy it for local dev without Docker. | INFO | Add a comment: `# For Docker: use 'db' as host. For local dev: use 'localhost'`. Or provide two commented variants. |
| I6 | `.gitignore`:86 | `.env` is gitignored at line 86, which is correct. However `.env.local`, `.env.production`, `.env.staging` patterns are not explicitly listed — relying on the `.env*` glob at line 44 which does cover them. This is adequate but fragile if someone adds a file like `env.local` (without dot prefix). | INFO | Consider also adding `env.local`, `env.production` (without dot) to `.gitignore` as a safety net. |

---

## Child Safety Assessment

| Category | Status | Notes |
|----------|--------|-------|
| No PII in client state | PASS | `game-progress-context.tsx` stores only `childId`, `name`, `age`, `color` — no email, no parent credentials |
| No external URLs in client code | PASS | No external links in reviewed files; `AI_ENDPOINT` is server-side only |
| No ads/tracking scripts | PASS | No analytics or ad SDKs in `package.json` |
| Parent gate enforced architecturally | FAIL | `POST /api/auth/pin` route exists but has no middleware guard. Parent gate is not enforced — any caller can skip it. When implemented, pin verification must gate all parent-facing routes at middleware level. |
| Child profile data isolation | FAIL | IDOR risk (S2) means a child's progress data is not isolated to their parent. Must be fixed before any real data is stored. |
| Content safety (AI endpoint) | WARN | `POST /api/ai/generate-questions` feeds `lessonId`, `gameType`, `difficulty` to an external AI endpoint. No output sanitization / content filtering is defined. AI-generated content must be validated before being stored in `AIQuestion.payload` or shown to children. |
| Sensitive data not logged | PASS (by absence) | No logging currently. Must enforce no-PII logging policy when logger is added. |

---

## OWASP Top 10 Checklist

| # | Vulnerability | Status |
|---|---------------|--------|
| 1 | Broken Access Control | FAIL — No middleware, IDOR vectors, client-controlled `correct` field |
| 2 | Cryptographic Failures | WARN — Schema shows `passwordHash`/`pinHash` (correct intent), but session token delivery mechanism undefined; no HttpOnly cookie policy yet |
| 3 | Injection | PASS (conditional) — Prisma ORM protects SQL; prompt injection risk at AI endpoint needs output sanitization |
| 4 | Insecure Design | FAIL — `correct: boolean` client-supplied is a design flaw, not an implementation bug |
| 5 | Security Misconfiguration | FAIL — No security headers, hardcoded credentials/endpoints in docker-compose, no CSP |
| 6 | Vulnerable Components | WARN — Dependency audit not run; `next@16.2.4` and `react@19.2.4` appear current but no `npm audit` output available |
| 7 | Auth Failures | FAIL — No rate limiting, no brute-force protection on login or PIN endpoints |
| 8 | Data Integrity Failures | WARN — No `AI_ENDPOINT` allowlist, AI output not sanitized |
| 9 | Logging Failures | FAIL — All errors silently swallowed, no structured logging |
| 10 | SSRF | WARN — AI endpoint called server-side without URL allowlist validation |

---

## Verdict

**BLOCKED — Critical issues must be resolved before Phase B/C implementation proceeds.**

The blocking issues are design-level, not implementation-level. If implementation begins now against these stubs, the critical flaws will be embedded in every future route:

1. **S1** — Middleware architecture must be established before any route is implemented
2. **S2** — IDOR prevention pattern must be documented in the implementation plan and coded into every child-scoped query
3. **S3** — `SubmitAttemptRequest.correct` must be removed from `lib/types/api.ts` now, before Phase B implements the attempts endpoint
4. **I1** — `Dockerfile` must exist before `docker-compose.yml` references `build: .`

Items S4–S15, P1–P11, I2–I6 are HIGH/WARN — they must be addressed in the same sprint as the routes they affect, not deferred.
